# Set Real IP from CloufFlare 

## Usage
 include /etc/nginx/cloudflare into nginx configuration.

 Drop syncCF.sh into /etc/cron.daily and chmod +x /etc/cron.daily/syncCF.sh

 magic is done  😉
## Prerequisites 
Install curl on your distro! 